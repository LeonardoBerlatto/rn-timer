import { StyleSheet } from 'react-native';

import { BACKGROUND_COLOR } from '../../styles/color-pallete';

export default StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        backgroundColor: BACKGROUND_COLOR,
        justifyContent: 'space-between'
    }
});
